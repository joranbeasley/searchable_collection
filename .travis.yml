language: python

before_install:
  - sudo pip install pyserial pytest coverage python-coveralls pytest-cov

script:
  - py.test tests --pep8 coveralls -v --cov coveralls --cov-report term-missing

after_success:
  - coveralls